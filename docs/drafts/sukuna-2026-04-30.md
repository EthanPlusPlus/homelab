# Sukuna — 2026-04-30
Direction: Even canon — Free run

## 1. Consistency

### Wording fixes

```diff
- # Even
-
- Receipt scanning iOS app. Scan a receipt, allocate who ate what, split the bill.
+ # Even
+
+ Receipt-splitting iOS app. Scan a receipt, allocate who ate what, split the bill.
```
(CLAUDE.md line 3 — the project description elsewhere is referred to as a "receipt-splitting app"; "Receipt scanning" understates scope. Minor, but it is the only narrative sentence in the canon.)

No other wording-level drift was found. The canon is too sparse for terminology to have diverged: only one product noun ("Even"), no service names, no cross-file repetition of concepts.

### Structural findings

- `CLAUDE.md` describes a workflow (steps 0–7) that references files and folders that do not exist: `proposed-ideas/`, `decisions/`, `runbooks/`, `architecture/` are declared in `STRUCTURE.md` but not present on disk. Step 2 ("Check docs/context/ — progress.md, recent-changes.md, constraints.md") is the only step grounded in real files.
- `CLAUDE.md` step 6 says `curl -X POST http://localhost:8000/index` — this assumes Prismo / context-server reachable on localhost, but step 5 ("add MCP server") points at `ubuntu-server.tail58b10c.ts.net:8001/mcp`. Two hosts, two ports, no explanation of which is the indexer vs. the MCP front-end. Stale or under-specified.
- `STRUCTURE.md` declares `proposed-ideas/` as "indexed" and `progress.md` "Next" section points the reader at "see proposed-ideas/ for upcoming directions" — but the folder does not exist. Dangling cross-reference.
- `STRUCTURE.md` lists `decisions/`, `runbooks/`, `architecture/` as indexed but none exist. The structure file is currently aspirational, not descriptive.
- Header style is consistent (`#` H1, `---` rule under intro, `##` sections). Frontmatter: none of the files use YAML frontmatter — consistent by absence.
- List conventions: bullets use `-` everywhere. Consistent.
- Empty-section placeholder convention drifts: `progress.md` uses `_(nothing currently in progress)_`, `_(nothing yet)_`; `recent-changes.md` uses `_(no changes yet)_`; `constraints.md` uses `_(none documented yet)_`; `open-questions.md` uses `_(none yet)_`. Four different phrasings for "empty." Pick one.
- `CLAUDE.md` has no `---` rule between sections, while `STRUCTURE.md` uses `---` heavily. Minor formatting divergence between the two top-level files.
- Missing top-level `README.md` or index. `CLAUDE.md` is the de-facto entry point but is named for the agent, not the human reader. Not a bug, just worth noting.

## 2. Observations

- The entire even canon is scaffolding. `CLAUDE.md`, `STRUCTURE.md`, and four placeholder files in `context/` and `open-questions.md` — that is it. Nothing has been recorded about the actual app: no decisions, no architecture, no constraints, no progress. The repo at `github.com/EthanPlusPlus/even` exists but no canon describes it. This is the dominant fact about the canon and warrants a decision about whether to backfill or write canon as decisions are made going forward.
- Contradiction between `CLAUDE.md` (workflow says "Retrieve context from MCP" first) and `STRUCTURE.md` (lists folders that the MCP cannot return content from because they do not exist). An agent following the workflow gets empty MCP results and has to fall back to direct reads — and there is nothing to read either.
- "Indexed and trusted" is asserted in `STRUCTURE.md` for `decisions/`, `runbooks/`, `architecture/`, `context/` but no decision record explains the trust model, how indexing happens, or what "trusted" means operationally. Concept assumed, never defined within the even canon (it may be defined in homelab canon, but even does not link to it).
- The dual-host setup (`ubuntu-server.tail58b10c.ts.net:8001/mcp` for MCP, `localhost:8000/index` for re-index) appears in `CLAUDE.md` with no decision record explaining the topology. Anyone reading just the even canon cannot tell if these are the same machine, what Prismo is, or why two ports.
- `progress.md` "Next" section defers to `proposed-ideas/` — but with the folder absent, this is a dangling pattern: progress is governed by a folder that has never existed. Either the convention is wrong (progress.md should hold "Next" itself) or the folder needs to be created with at least one idea.
- No decision record exists for the choice of iOS as the platform, for receipt-OCR strategy, for splitting model (per-item assignment vs. percentages vs. shares), or for backend/no-backend. These are the load-bearing decisions of a receipt-splitting app and the canon is silent on all of them. Largest gap.
- The empty-section placeholder phrasing drift (4 variants) is a pattern with no governing decision. Trivial, but symptomatic — STRUCTURE.md governs naming and folder semantics but not in-file conventions.


