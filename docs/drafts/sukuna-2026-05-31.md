# Sukuna — 2026-05-31
Direction: (1) Canon structure for V2; (2) Facets — new ones and improvements. See Section 3.

## 1. Consistency

### Wording fixes

**`architecture/overview.md`** — the entire file presents NAT/MASQUERADE as the live working
network architecture. This directly contradicts `constraints.md`, `runbooks/proxmox-setup.md`,
`runbooks/vm-internet-bootstrap.md`, and recent-changes (2026-05-22–25), all of which state NAT
never worked and Tailscale subnet router is the real path. The topology diagram is wrong.

```
- → Ubuntu Server VM (192.168.100.10) via NAT
+ → Ubuntu Server VM (192.168.100.10) — internet via Tailscale subnet router on Proxmox
+   (NAT/MASQUERADE does not work on this setup — see constraints.md / proxmox-setup.md)
```

The `## NAT Configuration` section should be reframed (or annotated) to say these iptables rules
exist but are inert; add a `## VM Internet Access (Tailscale Subnet Router)` section mirroring
`proxmox-setup.md` lines 20–43. As written, overview.md is the single most misleading doc in canon.

**`context/progress.md`** — the `## Complete` list still reads as if NAT is the working internet
path:

```
- Add NAT iptables rule (MASQUERADE via wlp2s0)
- ...
- Verify internet access from Ubuntu VM
+ - Add NAT iptables rule (MASQUERADE via wlp2s0) — note: inert, never provided internet (see constraints.md)
+ - Configure Tailscale subnet router on Proxmox for VM internet access
```

**`context/constraints.md` § Claude Reasoning → Session bootstrap** — still prescribes the V1
warm-up ritual, which Decision 016 + CLAUDE.md explicitly retired:

```
- **Session bootstrap (first message of every session):**
- Read recent-changes.md directly. Then run a warm-up before any task: ask one question...
+ **Session bootstrap (first message of every session):**
+ Superseded by Decision 016 — session hydration replaces the warm-up ritual. The
+ UserPromptSubmit hook emits [V2 HYDRATED CONTEXT] structurally. See decisions/016 and
+ memory/workflow.md step 0.
```

**`context-server/CLAUDE.md`** (component canon) — describes the storage stack as ChromaDB +
SQLite, superseded by the 2026-05-29 pgvector migration:

```
- Doc indexer — semantic search over markdown knowledge bases (ChromaDB + sentence-transformers)
- Code indexer — symbol and reference search over code (tree-sitter + SQLite + ChromaDB)
+ Doc indexer — semantic search over markdown (PostgreSQL + pgvector, sentence-transformers embeddings)
+ Code indexer — symbol and reference search over code (tree-sitter, PostgreSQL)
```

**`context-server/docs/context/progress.md` line 6** — `## ` summary line still lists the live
stack as "ChromaDB + sentence-transformers" while line 33 of the same file documents the migration
away from ChromaDB. Update line 6 to "PostgreSQL + pgvector + sentence-transformers."

**`scripts/sukuna.md` frontmatter** — `model: claude-opus-4-7` is stale (this run was invoked on
opus-4-8). Decision 021's ReviewItem contract example also hardcodes `"model_used": "claude-opus-4-7"`.
Neither is load-bearing, but both will keep drifting; suggest `model_used` examples use a
placeholder (`<coding_runtime model>`) rather than a pinned version.

### Structural findings

- **Decision frontmatter is two-tier.** Decisions 005, 007, 010, 012, 013, 017 carry only
  `id` / `status` / `record_type` — no `title:` or `date:`. Every decision from 020 onward carries
  the full set (`id` / `title` / `status` / `record_type` / `date`). The 2026-05-31 canon repair
  backfilled `id`/`status`/`record_type` but stopped short of `title`/`date`. `doctrine validate`
  passes (0/102) because those fields aren't required — but the inconsistency is real and will
  surface the moment any tooling sorts or renders by `date`/`title`. Backfill the six.
- **Two doc-count numbers in canon.** `v2-progress.md` says "821+ docs indexed"; Decision 031 says
  "1316+ indexed docs." Both are point-in-time and neither is wrong, but `v2-progress.md`'s 821 is a
  Phase 2 artifact presented as current. Drop the absolute count from v2-progress or annotate it as
  "(Phase 2 baseline)."
- **`wrap-up` facet is structurally inconsistent with its peers.** It has `facet.yaml` + `SKILL.md`
  but no `README.md` — the only one of five facets missing the human-readable design doc. Either add
  the README or document that README is optional (the STRUCTURE convention should state which files
  a facet requires).
- **`HOME.md` Dataview queries reference a flat `id`** (`SORT id ASC` over proposed-ideas) but
  decisions are sorted by `file.name` — inconsistent sort keys, and the proposed-ideas `id` is a
  quoted string (`"001"`) so numeric sort may misbehave. Minor; only affects Obsidian rendering.
- **No `architecture/`, `runbooks/`, `decisions/` folders exist in `prismo-ui` canon yet** even
  though its STRUCTURE.md documents all of them. Expected for a 1-day-old project — flag only so it
  isn't mistaken for missing content.
- **`drafts/` is frozen by Decision 021 but Sukuna is still writing to it** (this very file). This
  is the known, sanctioned interim per sukuna.md's Decision 021 annotation. Restating it here so the
  contradiction stays visible until proposed-idea 013's path replaces it — though note 013 is now
  *superseded* (see Section 2), so the replacement mechanism is itself in limbo.
