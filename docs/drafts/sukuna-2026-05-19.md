# Sukuna — 2026-05-19
Direction: Three topics. (1) Proposed-idea 014 (external synthesis / prior-art) — does it fit cleanly as a synthesis extension, or does it introduce scope that conflicts with the observation-week freeze? Check against Decision 021 and Decision 015. (2) The 3-mode synthesis model (internal / external / comparative) — is this consistent with the existing synthesis architecture? Does it belong as a proposed-idea annotation, a new decision, or something else? (3) The canon discipline section in ~/CLAUDE.md (Phase 1/Phase 2 MCP ritual) — this has been identified as V1 technical debt that contradicts the V2 masterplan principle of structural workflow enforcement over behavioral rituals. Flag it for cleanup and assess what, if anything, needs to replace it given current V2 build state.

## 1. Consistency

### Wording fixes

**`docs/proposed-ideas/013-sukuna-as-synthesis-consumer.md` — frontmatter `notes` misstates blocking dependency**

```diff
- notes: Sukuna 2026-05-17 self-identified this. Defer until doctrine-service Day-1 lands.
+ notes: Sukuna 2026-05-17 self-identified this. Defer until synthesis quality gates are calibrated via observation-week data.
```

The body correctly lists three reasons: doctrine-service Day-1, observation week calibration, and no urgency. The notes field only names reason 1, which is neither the most important nor the most actionable. Any surface that reads only frontmatter (MCP queries, lifecycle tools, summaries) would infer the wrong gate.

---

### Structural findings

- **`docs/architecture/v2-masterplan.md` (line 459) and `docs/architecture/v2-roadmap.md` (lines 684, 1335) still contain `#### Sukuna v2` / `# Initiative 2.5 — Sukuna v2` section headings.** The 2026-05-17 cleanup annotated Decisions 013 and 017 and cleaned the Phase 1 contract docs, but the foundational architecture docs were not touched. These are either historical record (acceptable if marked as such) or ghost references (should be annotated). The roadmap in particular still describes Sukuna v2 as a separate Layer 2 service with its own initiative, which contradicts the retirement recorded in v2-progress.md.

- **`docs/proposed-ideas/011-self-learning-feedback-loop.md` (line 22) links to `[[003-knowledge-base-source-of-truth|Decision 003]]`.** Decisions 001–004 no longer exist in the decisions directory. This is a dead cross-reference. Any link-traversal tool or doc-graph query will fail on it.

- **`~/CLAUDE.md` Session Bootstrap (step 2) and `docs/memory/workflow.md` (step 0) describe different warm-up triggers.** CLAUDE.md says: always ask one targeted question and repeat 2–4 exchanges (with a skip condition for context-rich messages). workflow.md says: if `current_focus` is set, proceed directly; if empty, ask one question. These are structurally different gates. CLAUDE.md triggers warm-up even when structural hydration already set `current_focus`, making Decision 016's improvement advisory rather than enforced for sessions governed by CLAUDE.md.

- **`docs/proposed-ideas/014-external-synthesis-prior-art.md` introduces a new ReviewItem `artifact_type` value (`prior-art-report`) without flagging that the Decision 021 schema requires extension.** Decision 021 defines `artifact_type` as a closed set: `"decision | proposed-idea | runbook-update | annotation | recent-changes-entry"`. Adding `prior-art-report` is a schema change. The proposed-idea treats it as a trivial addition; it isn't — it requires updating the ReviewItem contract, the review CLI rendering, and any preflight validators that enumerate valid types.
